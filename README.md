# Morf 3.4.2 button rescue

This build keeps the Morf 3.4 syntax/UI and adds a pre-app button rescue layer so Generate, Translate, Import, Export, Pick Random, Alphabetize, and Settings buttons still work even if a later app listener fails.

Upload `index.html` to GitHub Pages or open the standalone HTML directly.
